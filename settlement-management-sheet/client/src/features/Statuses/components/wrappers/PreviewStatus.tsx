import React, { useState } from 'react';

import { Box } from '@mui/material';

const PreviewStatus = ({ id, mode, side, tabId }) => {
  return <Box>I'm preview status!</Box>;
};

export default PreviewStatus;
